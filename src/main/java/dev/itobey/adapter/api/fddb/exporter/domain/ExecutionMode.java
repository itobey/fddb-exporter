package dev.itobey.adapter.api.fddb.exporter.domain;

/**
 * ExecutionMode enum represents the different modes in which the application can run for telemetry purposes.
 */
public enum ExecutionMode {
    KUBERNETES,
    CONTAINER,
    JAR
}
